# Heavy PDF assets are skipped by default to keep Netlify CLI deploys
# from scanning/uploading ~50 GB of rarely changing files.
# To publish changed PDFs or thumbnails, run:
#   bash scripts/deploy.sh --include-pdfs
pdfs/
pdf-thumbnails/
