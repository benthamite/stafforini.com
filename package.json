{
  "name": "stafforini.com",
  "private": true,
  "scripts": {
    "build": "hugo --minify && npx pagefind --site public",
    "dev": "python3 scripts/generate-citing-notes.py; hugo server --navigateToChanged",
    "reindex": "bash scripts/build-search-index.sh",
    "index": "npx pagefind --site public"
  },
  "devDependencies": {
    "pagefind": "^1.4.0"
  }
}
